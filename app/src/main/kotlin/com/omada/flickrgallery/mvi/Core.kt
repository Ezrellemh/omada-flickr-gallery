package com.omada.flickrgallery.mvi

/** Represents the state of the UI. */
interface ViewState

/** Represents actions coming from the UI (clicks, inputs, etc.). */
interface Action

/** Represents one-off UI events (snackbar, navigation, etc.). */
interface Effect
